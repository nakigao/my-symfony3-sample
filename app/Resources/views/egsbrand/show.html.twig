{% extends 'admin/layout.html.twig' %}

{% block content %}
    <div class="row">
        <div class="col-sm-12">
            <h1>Egsbrand</h1>
            <table class="table">
                <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ egsBrand.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ egsBrand.name }}</td>
                </tr>
                <tr>
                    <th>Namekana</th>
                    <td>{{ egsBrand.nameKana }}</td>
                </tr>
                <tr>
                    <th>Url</th>
                    <td>{{ egsBrand.url }}</td>
                </tr>
                </tbody>
            </table>
            <ul>
                <li>
                    <a href="{{ path('admin_egsbrand_index') }}">Back to the list</a>
                </li>
                {#<li>#}
                {#<a href="{{ path('admin_egsbrand_edit', { 'id': egsBrand.id }) }}">Edit</a>#}
                {#</li>#}
                {#<li>#}
                {#{{ form_start(delete_form) }}#}
                {#<input type="submit" value="Delete">#}
                {#{{ form_end(delete_form) }}#}
                {#</li>#}
            </ul>
        </div>
    </div>
{% endblock %}
